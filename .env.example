NODE_ENV=development
PORT=4000

# Remote MySQL (use strong secrets in production; never commit .env)
DB_HOST=82.25.121.166
DB_PORT=3306
DB_USER=u954141192_better_talk
DB_PASSWORD=
DB_NAME=u954141192_better_talk

# Optional: set true if your host requires TLS (plan used plain connection)
DB_SSL=false

JWT_ACCESS_SECRET=change-me-min-32-chars-access-secret
JWT_REFRESH_SECRET=change-me-min-32-chars-refresh-secret
JWT_ACCESS_EXPIRES=15m
JWT_REFRESH_EXPIRES=7d
JWT_PREAUTH_EXPIRES=5m

# Optional: absolute URL prefix for export job.file_url (e.g. http://localhost:4000)
PUBLIC_BASE_URL=

# Optional: directory for generated CSV exports (default: ./exports)
EXPORT_FILES_DIR=

# Optional: directory for uploaded incident/field media files (default: ./uploads)
UPLOAD_FILES_DIR=

# Optional: Google Maps API key for backend geocoding/place integrations
GOOGLE_MAPS_API_KEY=

CORS_ORIGINS=http://localhost:3000
